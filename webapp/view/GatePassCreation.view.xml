<mvc:View
	controllerName="zaudgpms.audhatham.com.controller.GatePassCreation"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:form="sap.ui.layout.form"
	xmlns:core="sap.ui.core"
	xmlns:unified="sap.ui.unified"
	xmlns:layout="sap.ui.layout"
	height="100%">
	<Page
		id="gatePassCreationPage"
		title="{= ${gp>/GatePassNo} ? 'Gate Pass Details - ' + ${gp>/GatePassNo} : 'Create Gate Pass' }"
		showNavButton="{= !${gp>/editable} }"
		navButtonPress=".onNavBackToList"
		class="gpmsPage">
		<content>
			<VBox class="gpmsContentWrapper sapUiMediumMargin">

				<!-- ─── SECTION 1 · Control Parameters ──────────── -->
				<Panel class="gpmsSection" expandable="false">
					<headerToolbar>
						<Toolbar class="gpmsSectionBar gpmsAccentBar">
							<core:Icon src="sap-icon://settings" class="gpmsSectionBarIcon"/>
							<Label text="Control Parameters" class="gpmsSectionBarLabel"/>
						</Toolbar>
					</headerToolbar>
					<layout:Grid defaultSpan="XL4 L4 M4 S12" vSpacing="1" hSpacing="1">
						
						<!-- Direction -->
						<VBox>
							<Label text="Direction" required="true" class="gpmsFieldLabel"/>
							<RadioButtonGroup 
								columns="2" 
								selectedIndex="{path: 'gp>/DirectionIndex', mode: 'TwoWay'}" 
								select=".onDirectionChange" 
								editable="{gp>/editable}">
								<buttons>
									<RadioButton text="Inward" />
									<RadioButton text="Outward" />
								</buttons>
							</RadioButtonGroup>
						</VBox>

						<!-- Type -->
						<VBox>
							<Label text="Type" required="true" class="gpmsFieldLabel"/>
							<RadioButtonGroup 
								columns="2" 
								selectedIndex="{path: 'gp>/TypeIndex', mode: 'TwoWay'}" 
								select=".onTypeChange" 
								editable="{gp>/editable}">
								<buttons>
									<RadioButton text="Returnable" />
									<RadioButton text="Non Returnable" />
								</buttons>
							</RadioButtonGroup>
						</VBox>

						<!-- Entry Mode -->
						<VBox>
							<Label text="Entry Mode" required="true" class="gpmsFieldLabel"/>
							<RadioButtonGroup 
								columns="2" 
								selectedIndex="{path: 'gp>/EntryModeIndex', mode: 'TwoWay'}" 
								select=".onEntryModeChange" 
								editable="{gp>/editable}">
								<buttons>
									<RadioButton text="System / PO" />
									<RadioButton text="Manual" />
								</buttons>
							</RadioButtonGroup>
						</VBox>

					</layout:Grid>
				</Panel>

				<!-- ─── SECTION 2 · General & Header Information ──────────── -->
				<Panel class="gpmsSection" expandable="false">
					<headerToolbar>
						<Toolbar class="gpmsSectionBar">
							<core:Icon src="sap-icon://document-text" class="gpmsSectionBarIcon"/>
							<Label text="Header Information" class="gpmsSectionBarLabel"/>
							<ToolbarSpacer/>
							<ObjectStatus 
								text="{= ${gp>/GatePassNo} ? ${gp>/Status} : 'Draft' }" 
								state="{= ${gp>/Status} === 'Approved' ? 'Success' : 'Warning' }" 
								class="gpmsBadge"/>
						</Toolbar>
					</headerToolbar>
					<layout:Grid defaultSpan="XL4 L4 M4 S12" vSpacing="1" hSpacing="1">
						
						<VBox>
							<Label text="Plant Code" required="true" class="gpmsFieldLabel"/>
							<ComboBox
								id="plantSelect"
								selectedKey="{gp>/Plant}"
								items="{path: 'plants>/results'}"
								selectionChange=".onPlantChange"
								change=".onComboBoxPlantChange"
								placeholder="Select Plant code..."
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%">
								<core:ListItem key="{plants>Plant}" text="{plants>Plant} - {plants>PlantName}"/>
							</ComboBox>
						</VBox>

						<VBox>
							<Label text="Date" required="true" class="gpmsFieldLabel"/>
							<DatePicker
								id="gpDate"
								dateValue="{gp>/gpDate}"
								displayFormat="dd-MM-yyyy"
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%"/>
						</VBox>

						<VBox>
							<Label text="Gatepass Number" class="gpmsFieldLabel"/>
							<Input 
								value="{gp>/GatePassNo}" 
								editable="false" 
								class="gpmsFieldInput gpmsFieldInputReadonly" 
								width="100%"/>
						</VBox>

						<VBox>
							<layoutData>
								<layout:GridData span="XL8 L8 M12 S12"/>
							</layoutData>
							<Label text="Comments/Remarks (Max 100 Char)" class="gpmsFieldLabel"/>
							<TextArea
								value="{gp>/Remarks}"
								placeholder="Enter remarks..."
								rows="2"
								maxLength="100"
								showExceededText="true"
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%"/>
						</VBox>

					</layout:Grid>
				</Panel>

				<!-- ─── SECTION 3 · Supplier & Transport Details ────────── -->
				<Panel class="gpmsSection" expandable="false">
					<headerToolbar>
						<Toolbar class="gpmsSectionBar gpmsAccentBar">
							<core:Icon src="sap-icon://shipping-status" class="gpmsSectionBarIcon"/>
							<Label text="Supplier &amp; Transport Details" class="gpmsSectionBarLabel"/>
						</Toolbar>
					</headerToolbar>
					<layout:Grid defaultSpan="XL4 L4 M4 S12" vSpacing="1" hSpacing="1">
						
						<VBox>
							<Label text="Supplier" required="true" class="gpmsFieldLabel"/>
							<ComboBox
								id="vendorInput"
								selectedKey="{gp>/vendor}"
								placeholder="Search supplier..."
								selectionChange=".onVendorSelect"
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%"
								items="{path: 'vendors>/results', templateShareable: false}">
								<core:ListItem key="{vendors>Vendor}" text="{vendors>VendorName}" additionalText="{vendors>Vendor}"/>
							</ComboBox>
						</VBox>

						<VBox>
							<Label text="Supplier Name" class="gpmsFieldLabel"/>
							<Input
								value="{gp>/vendorName}"
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%"/>
						</VBox>

						<VBox visible="{= ${gp>/EntryModeIndex} === 0 }">
							<Label text="PO Number" required="{= ${gp>/EntryModeIndex} === 0 }" class="gpmsFieldLabel"/>
							<Input
								value="{gp>/PurchaseOrder}"
								placeholder="Enter PO number..."
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%"/>
						</VBox>

						<VBox>
							<Label text="Customer Invoice number" class="gpmsFieldLabel"/>
							<Input
								value="{gp>/CustomerInvoice}"
								placeholder="Enter Customer Invoice number..."
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%"/>
						</VBox>

						<VBox>
							<Label text="Mode of dispatch" class="gpmsFieldLabel"/>
							<Select
								selectedKey="{gp>/ModeOfDispatch}"
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%">
								<core:Item key="Road" text="By Road"/>
								<core:Item key="Rail" text="By Rail"/>
								<core:Item key="Air" text="By Air"/>
								<core:Item key="Courier" text="By Courier"/>
							</Select>
						</VBox>

						<VBox>
							<Label text="Vehicle Number" class="gpmsFieldLabel"/>
							<Input
								value="{gp>/VehicleNo}"
								placeholder="Enter Vehicle number..."
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%"/>
						</VBox>

						<VBox>
							<Label text="Driver Name" class="gpmsFieldLabel"/>
							<Input
								value="{gp>/DriverName}"
								placeholder="Enter Driver name..."
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%"/>
						</VBox>

						<!-- Inward Dropdown -->
						<VBox visible="{= ${gp>/DirectionIndex} === 0 }">
							<Label text="Inward Category" class="gpmsFieldLabel"/>
							<Select
								selectedKey="{gp>/InwardCategory}"
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%">
								<core:Item key="01-Purchase order" text="01-Purchase order"/>
								<core:Item key="02-Customer return" text="02-Customer return"/>
								<core:Item key="03-Samples" text="03-Samples"/>
								<core:Item key="04-Others" text="04-Others"/>
							</Select>
						</VBox>

						<!-- Outward Dropdown -->
						<VBox visible="{= ${gp>/DirectionIndex} === 1 }">
							<Label text="Outward Category" class="gpmsFieldLabel"/>
							<Select
								selectedKey="{gp>/OutwardCategory}"
								editable="{gp>/editable}"
								class="gpmsFieldInput"
								width="100%">
								<core:Item key="01-Service maintenance" text="01-Service maintenance"/>
								<core:Item key="02-Customer sales" text="02-Customer sales"/>
								<core:Item key="03-Vendor Returns" text="03-Vendor Returns"/>
								<core:Item key="04-Others" text="04-Others"/>
							</Select>
						</VBox>

					</layout:Grid>
				</Panel>

				<!-- ─── SECTION 4 · Table of Items ────────────── -->
				<Panel class="gpmsSection" expandable="false">
					<headerToolbar>
						<Toolbar class="gpmsSectionBar">
							<core:Icon src="sap-icon://grid" class="gpmsSectionBarIcon"/>
							<Label text="Item Information" class="gpmsSectionBarLabel"/>
							<ToolbarSpacer/>
							<Button
								text="Add Row"
								icon="sap-icon://add"
								press=".onAddItem"
								visible="{gp>/editable}"
								class="gpmsAddBtn"/>
						</Toolbar>
					</headerToolbar>
					<Table
						id="itemsTable"
						items="{path: 'gp>/items', templateShareable: false}"
						noDataText="No items added."
						class="gpmsItemTable">
						<columns>
							<Column width="3rem" hAlign="Center"><Text text="#" class="gpmsHeaderText"/></Column>
							
							<!-- Material (SAP Code) - Visible if Entry Mode is System -->
							<Column width="11rem" visible="{= ${gp>/EntryModeIndex} === 0 }">
								<Text text="Material" class="gpmsHeaderText"/>
							</Column>
							
							<!-- Material Description - Always Visible -->
							<Column width="15rem">
								<Text text="Material Description" class="gpmsHeaderText"/>
							</Column>
							
							<!-- Qty - Always Visible -->
							<Column width="7rem" hAlign="Right">
								<Text text="Qty" class="gpmsHeaderText"/>
							</Column>
							
							<!-- Uom - Always Visible -->
							<Column width="6rem" hAlign="Center">
								<Text text="UOM" class="gpmsHeaderText"/>
							</Column>
							
							<!-- Expected Returnable Date -->
							<!-- Visible if Outward AND ((RGP and System) OR (NRGP and Manual)) -->
							<Column width="11rem" visible="{= ${gp>/DirectionIndex} === 1 &amp;&amp; ((${gp>/EntryModeIndex} === 0 &amp;&amp; ${gp>/TypeIndex} === 0) || (${gp>/EntryModeIndex} === 1 &amp;&amp; ${gp>/TypeIndex} === 1)) }">
								<Text text="Expected Returnable date" class="gpmsHeaderText"/>
							</Column>

							<!-- Received Qty - Visible for Inward Returnable (RGP) -->
							<Column width="7rem" hAlign="Right" visible="{= ${gp>/DirectionIndex} === 0 &amp;&amp; ${gp>/TypeIndex} === 0 }">
								<Text text="Received Qty" class="gpmsHeaderText"/>
							</Column>

							<!-- Return Date - Visible for Inward Returnable (RGP) -->
							<Column width="11rem" visible="{= ${gp>/DirectionIndex} === 0 &amp;&amp; ${gp>/TypeIndex} === 0 }">
								<Text text="Return date" class="gpmsHeaderText"/>
							</Column>

							<!-- Rate & Amount (Optional / Hidden but kept for OData compatibility) -->
							<Column width="7rem" hAlign="Right" visible="false">
								<Text text="Rate" class="gpmsHeaderText"/>
							</Column>
							<Column width="8rem" hAlign="Right" visible="false">
								<Text text="Amount" class="gpmsHeaderText"/>
							</Column>

							<!-- Actions -->
							<Column width="4rem" hAlign="Center" visible="{gp>/editable}"><Text text="" class="gpmsHeaderText"/></Column>
						</columns>
						<items>
							<ColumnListItem vAlign="Middle" class="gpmsItemRow">
								<cells>
									<Text text="{gp>sno}" class="gpmsSnoText"/>

									<!-- Material Input (System Mode) -->
									<Input
										value="{gp>material}"
										showValueHelp="true"
										valueHelpRequest=".onMaterialValueHelp"
										editable="{gp>/editable}"
										class="gpmsTableInput"
										width="100%"
										placeholder="F4..."/>

									<!-- Material Description -->
									<Input
										value="{gp>materialName}"
										editable="{= ${gp>/EntryModeIndex} === 1 &amp;&amp; ${gp>/editable} }"
										class="gpmsTableInput"
										width="100%"
										placeholder="Enter description..."/>

									<!-- Quantity -->
									<Input 
										value="{gp>quantity}" 
										type="Number" 
										textAlign="Right" 
										editable="{gp>/editable}"
										liveChange=".onQtyRateChange" 
										class="gpmsTableInput"/>

									<!-- UOM -->
									<Input 
										value="{gp>uom}" 
										editable="{= ${gp>/EntryModeIndex} === 1 &amp;&amp; ${gp>/editable} }" 
										class="gpmsTableInput" 
										textAlign="Center" 
										placeholder="UOM..."/>

									<!-- Expected Returnable Date -->
									<DatePicker
										dateValue="{gp>expectedReturnableDate}"
										displayFormat="dd-MM-yyyy"
										editable="{gp>/editable}"
										class="gpmsTableInput"
										width="100%"/>

									<!-- Received Qty -->
									<Input 
										value="{gp>receivedQty}" 
										type="Number" 
										textAlign="Right" 
										editable="{gp>/editable}"
										class="gpmsTableInput"/>

									<!-- Return Date -->
									<DatePicker
										dateValue="{gp>returnDate}"
										displayFormat="dd-MM-yyyy"
										editable="{gp>/editable}"
										class="gpmsTableInput"
										width="100%"/>

									<!-- Rate (Hidden) -->
									<Input value="{gp>rate}" type="Number" textAlign="Right" liveChange=".onQtyRateChange" class="gpmsTableInput"/>

									<!-- Amount (Hidden) -->
									<Text text="{gp>amount}" class="gpmsAmountText" width="100%"/>

									<Button icon="sap-icon://delete" type="Transparent" press=".onRemoveItem" class="gpmsDeleteBtn"/>
								</cells>
							</ColumnListItem>
						</items>
					</Table>
				</Panel>

			</VBox>
		</content>
		<footer>
			<OverflowToolbar class="gpmsFooterBar">
				<Button 
					text="Back to List" 
					icon="sap-icon://back" 
					press=".onNavBackToList" 
					class="gpmsClearBtn" 
					visible="{= !${gp>/editable} }"/>
				<ToolbarSpacer/>
				<Button 
					text="Clear All" 
					icon="sap-icon://clear-all" 
					press=".onClear" 
					class="gpmsClearBtn" 
					visible="{gp>/editable}"/>
				<Button 
					text="Print Gate Pass" 
					icon="sap-icon://print" 
					press=".onPrint" 
					type="Emphasized"
					class="gpmsSubmitBtn sapUiSmallMarginEnd"/>
				<Button 
					text="Submit Gate Pass" 
					icon="sap-icon://paper-plane" 
					press=".onSubmit" 
					class="gpmsSubmitBtn" 
					visible="{gp>/editable}"/>
			</OverflowToolbar>
		</footer>
	</Page>
</mvc:View>
